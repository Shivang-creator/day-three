[project]
name = "day-three"
version = "0.1.0"
description = "Day Three — postnatal follow-up agent (All Things Agentic hackathon)"
requires-python = ">=3.12"
readme = "README.md"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
